import * as React from 'react';
import { SvgWrapper } from '@ions/icon/wrapper/SvgWrapper';

export const {{pascalCase name}}Icon: React.FunctionComponent< import('../../ions/icon/types').IconProps> = (props:
  import('../../ions/icon/types').IconProps): JSX.Element => (
  <SvgWrapper {...props}>
    <React.Fragment>
      {{{path}}}
    </React.Fragment>
  </SvgWrapper>
  );
